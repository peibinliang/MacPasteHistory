# V1.0.3 核心稳定版本开发记录

## 1. 版本目标

V1.0.3 用于完成 `docs/roadmap/V1.0-V1.4-PRODUCT_ROADMAP.md` 中 V1.0.x 的核心稳定工作。范围由 OpenSpec change `stabilize-v1-0-3-core-foundations` 管理，不提前实现 V1.1 的 Slots、Tags、Smart Collections、App Rules 或 FTS5。

## 2. 基线证据

基线提交：`53dc0a8`（从 `origin/main` 快进同步后创建 `release/v1.0.3`）。

执行日期：2026-08-11。

| 检查 | 结果 | 证据摘要 |
| --- | --- | --- |
| Xcode 文件引用 | PASS | 检查 189 个 Swift 引用，缺失 0 个 |
| 隐私日志扫描 | PASS | 扫描 118 个 Swift 文件、28 个 Logger 调用，直接 console 调用 0 个 |
| 完整单元/集成测试 | PASS | 318 个测试，0 失败，0 unexpected |
| Debug Build | PASS | macOS arm64，`BUILD SUCCEEDED` |
| OpenSpec strict validation | PASS | 使用固定版本 CLI：`npx --yes @fission-ai/openspec@1.8.0 validate stabilize-v1-0-3-core-foundations --strict`；change valid，4/4 规划 artifacts 完成 |

首次在受限沙箱内运行 `xcodebuild test` 时，Xcode 无权写入用户级 `DerivedData/SourcePackages`，退出码为 74。该问题属于执行环境权限，不是编译或测试失败；在获得 Xcode 所需目录权限后，同一命令成功执行并得到上述 318/318 结果。

## 3. V1.0.2 数据库与版本基线

- Bundle short version：`1.0.2`。
- Bundle build：`4`。
- 最低系统：macOS 14.0。
- 当前 schema migration：V1–V4。
- V3 增加搜索/分类、捕获事件、使用统计、OCR 和派生记录字段及索引。
- V4 增加 `ai_token_usage` 表和索引。
- 现有测试覆盖代码生成的 V2 → V3/V4 升级、V3/V4 幂等和失败回滚，以及全新数据库初始化。
- 仓库目前没有从已发布 V1.0.2 build 4 应用生成并脱敏保存的 `.db`/`.sqlite` fixture；因此本阶段只完成 fixture 盘点，不能将真实 build 4 数据库升级标记为已验证。Task 8.1 必须补充代表性 build 4 fixture 后再关闭升级门禁。

## 4. V1.0.3 升级假设

1. V1.0.3 必须直接打开有效的 V1.0.2 build 4 数据库，不允许通过删除或重建 `clipboard.db` 完成升级。
2. 已发布的 V1–V4 migration 不得修改；如确需 schema 变化，只能追加下一个递增 migration。
3. 捕获来源一致性、Sensitive Detector V2、搜索取消、PasteCoordinator 和 ViewModel 拆分默认不需要 schema 变化。
4. Storage Reconciliation 首先以 inventory/plan 模式建立证据；只有可证明属于应用管理目录且修复动作幂等时才允许变更文件。
5. SQLite WAL 不是既定交付，必须与当前 journal mode 使用同一套并发、重开、回滚和旧库升级测试比较后决定。

## 5. 回滚边界

- 每个 TDD Task 使用独立、可审查 commit，RED 与 GREEN checkpoint 均必须位于 `release/v1.0.3` 当前历史。
- 无 schema 变化的任务可以按 commit 回滚，不得删除用户数据库或图片。
- 如果追加 migration，回滚旧应用时必须仍能读取既有核心表；不得以降级脚本删除用户字段或数据。
- Tag、签名、公证、Sparkle 包、GitHub Release 和 appcast 不属于本开发任务；在这些外部动作发生前，版本回滚只涉及本地 Git 分支。

## 6. 当前状态

- OpenSpec requirements/design/tasks：已创建，并通过上文版本固定的 OpenSpec strict validation 命令。
- 基线 QA：通过。
- Clipboard Source Capture RED：通过。`ClipboardMonitorTests` 共执行 14 个测试，新增的文本/图片来源竞态测试因来源 provider 被调用 2 次、记录错误使用第二个前台应用而失败；blocked-app 快速切换回归测试通过。该失败与目标缺陷一致。
- Clipboard Source Capture GREEN：通过。`ClipboardMonitorTests` 与 `ClipboardHistoryRepositoryTests` 共 34 个测试、0 失败；Xcode 检查 190 个 Swift 引用、缺失 0 个。
- 生产代码变更：新增不可变 `ClipboardCaptureContext`，并让 monitor、history record 与 capture event 共享单次来源和捕获时间。
- Source Capture 回归补充：覆盖来源不可用时保持单一 unknown snapshot，以及重复文本捕获的 history/event 时间一致性；同步更新文本历史开发文档与隐私政策。
- Source Capture Review：确认 pipeline 内一致性实现正确，同时识别两个剩余边界。轮询前切换 App 无法从 pasteboard 还原真实复制来源，记录为 PD-005；秒级 SQLite 时间导致的同秒重复捕获排序风险纳入 Task 8.1，不在本任务中无证据改写时间格式。
- Source Capture 完整回归：322 个测试、0 失败；隐私日志扫描继续通过。
- Sensitive Detector V2 RED：新增密码、API token、Authorization、Luhn 卡号、身份值与开发者标识符样本矩阵。当前实现的宽泛长字符串/数字正则在负例中产生 6 个预期失败；测试目标共 2 个测试、6 个断言失败，确认 RED 有效。
- Sensitive Detector V2 GREEN：检测结果改为类别、置信度与不含原文的安全 reason code；高可信结果才阻止持久化。凭据/上下文秘密使用标签边界，卡号使用 Luhn，18 位身份值校验出生日期与校验位，并保留可注入的本地用户规则边界。检测器 10 个测试与 ClipboardMonitor 隐私回归全部通过。
- Sensitive Detector V2 隐私收口：日志扫描 120 个 Swift 文件、28 个 Logger 调用，直接 console 调用 0 个，确认检测器不记录或传输待检文本；ClipboardMonitor 继续只记录通用跳过状态。分类结果当前未暴露到 UI，因此三种本地化无需新增分类文案；现有过滤开关与关闭风险提示在 English、简体中文、繁体中文中均保留。检测器、ClipboardMonitor 与 LocalizationCoverage 定向测试全部通过。
- Storage Reconciliation RED：新增 7 个 scan/plan/apply 契约测试，覆盖缺失原图、普通 orphan 保留、缺失缩略图、损坏原图、24 小时应用自有临时文件、外部路径不可信、单项失败隔离与重复运行幂等。当前因 `StorageReconciliationPlan` 和 `StorageReconciliationService` 尚不存在而编译失败，确认 RED 有效；Xcode 文件引用检查 193 个 Swift 引用、缺失 0 个。
- Storage Reconciliation report-only GREEN：新增无 UI 分页上限的图片记录 inventory、canonical root 校验、regular-file/symlink 边界、typed issues 与 typed safe actions。六个 scan/plan 测试通过；阶段内 `reconcile()` 只返回非敏感计数，不执行磁盘变更。
- Storage Reconciliation safe-action GREEN：只启用缺失缩略图重建和超过 24 小时的 `mph-image-*.tmp` 删除。执行前重新校验当前 DB 引用、canonical root、文件类型、symlink、修改时间与未引用状态；普通 orphan、缺失/损坏原图和不可信路径保持不变。对账与图片存储共 10 个测试通过，重复运行第二次完成动作数为 0。
- Storage Reconciliation 生命周期收口：同步 retention cleanup 完成后，将对账派发到 utility queue，并为其单独打开 read-only SQLite connection；主线程不等待图片解码。新增 canonical `temporary` 目录、仅计数的完成/失败日志，以及单目录扫描失败仍返回摘要的 QA。Storage Reconciliation、ApplicationSupport、ImageStorage 定向测试与隐私日志扫描全部通过。
- Search Task Lifecycle RED：新增可控 actor 测试依赖，覆盖旧 Task 显式取消、最新结果所有权、full search 等待期间保留 immediate 结果、最新错误保留和 `j → js → json` 快速输入。现有 ViewModel 未持有搜索 Task，新增 3 个生命周期测试均按预期失败，确认 RED 有效。
- Search Task Lifecycle GREEN：ViewModel 现在保留一个 `Task<Void, Never>`，新输入先取消旧 Task 并递增 request ID；所有 immediate/full 结果、错误和 loading 收口都要求仍属于最新 request。SearchCoordinator generation 校验继续保留。SearchViewModel 与 SearchCoordinator 定向测试全部通过。
- Search Task Lifecycle 回归收口：SearchCandidateProvider/SQLBuilder、Coordinator、FilterMerger、Performance、QueryParser、Ranker、SuggestionProvider 与 ViewModel 全部搜索测试通过；架构文档明确 Task cancellation、request ownership 和 actor generation 的分工。
- Unified Paste Coordination RED：新增 8 个 outcome-driven 服务测试，覆盖历史文本/图片、action output、Clipboard 写入失败、Automatic Paste 关闭、Accessibility 缺失、目标 App 缺失、dispatch 成败、取消和 usage accounting 精确一次。当前因 PasteCoordinator 及其窄协议边界尚不存在而编译失败，确认 RED 有效；Xcode 文件引用检查 196 个 Swift 引用、缺失 0 个。
- Unified Paste Coordination service GREEN：新增 typed `PasteRequest`/`PasteOutcome` 模型和 MainActor `PasteCoordinator`，通过窄协议组合 clipboard writer、Automatic Paste readiness、目标激活、paste command、usage recorder 与可控 dispatch delay。8 个 PasteCoordinator 服务测试全部通过，验证 fallback 不派发粘贴、dispatch 成功只计 paste、clipboard-only/cancel 只计 reuse、写入失败不计数。
- Unified Paste Coordination integration GREEN：`MainPanelView` 的历史粘贴和 action output 粘贴路径改为生成 `PasteRequest` 并委托 `PasteCoordinator`；`ClipboardHistoryViewModel` 只保留 payload 构造、clipboard writer、usage recorder 与 outcome-to-error 映射职责。关闭面板动作在 clipboard 写入成功且 ready 分支准备 dispatch 时执行，保留快捷键入口和现有 toast/权限提醒。ClipboardHistoryViewModel 与 PasteCoordinator 定向测试 27 个通过，并覆盖 ready 分支只收口一次、clipboard-only 分支不收口；Xcode 文件引用检查 198 个、缺失 0 个。
- Unified Paste Coordination 文档与反馈收口：架构、Automatic Paste 配置、用户指南与隐私政策同步 typed outcome、手动 fallback、取消与精确一次记账边界。没有新增用户可见状态或控件；沿用的权限弹窗、打开系统设置、稍后、手动粘贴与 dispatch 失败文案均已存在于 English、简体中文和繁体中文资源。取消发生在 clipboard 写入成功之后时，`PasteOutcomeFeedback` 也会展示手动粘贴提示。当前 HEAD 上 PasteCoordinator、ClipboardHistoryViewModel、LocalizationCoverage 与 AccessibilityPresentation 定向测试共 35 个、0 失败。隐私日志扫描检查 124 个 Swift 文件、29 个 Logger 调用、直接 console 调用 0 个，Info.plist 两项用途说明校验无违规。
- History ViewModel characterization：现有列表分页、筛选、搜索 lifecycle、收藏、文本/图片恢复、派生记录选择和键盘策略测试全部通过；新增文本删除刷新、清除文本但保留图片、缺失图片 paste request 反馈和 typed clipboard-write failure 不覆盖图片专用反馈。Task 7 重构将以这些行为作为兼容基线。
- History ViewModel 协作者拆分：新增可注入的 `HistoryListCoordinating`、`SearchTaskLifecycleManaging` 与 `HistorySelectionManaging` 窄边界，分别持有普通/排名分页、Task/request ownership 和派生记录选择；`ClipboardHistoryViewModel` 保留 SwiftUI 已绑定的 published API、命令与反馈适配。列表刷新现在会先取消并失效正在运行的搜索，防止通知或 CRUD 刷新后旧 full response 覆盖新列表。ViewModel、Search 与 PasteCoordinator 定向回归全部通过，Xcode 文件引用 199 个、缺失 0 个。
- History ViewModel MainActor 收口：搜索 Task 不再在 await 期间强持有 ViewModel；面板替换或释放 facade 会让 lifecycle deinit 取消未完成任务。新增“运行中列表刷新取消旧搜索且保留 repository 结果”和“运行中释放 ViewModel 可立即释放并取消 Task”测试，连同 `j → js → json` 快速输入、最新错误/结果所有权全部通过。架构文档明确 Views 只依赖 facade，源码检查确认 `MainPanelView` 未新增 SQLite 或粘贴记账业务。
- SQLite 同秒排序 RED：新增同一秒内依次捕获 A、B、再次捕获 A 的 repository 测试，要求 A 按真实亚秒重捕获时间回到首位并可恢复该时间。现有秒级 formatter 将三次时间截断为同值，查询退回 `id DESC`，测试按预期失败，确认此前独立审查发现的缺陷可复现。
- SQLite 亚秒时间 GREEN：新增向后兼容的 `SQLiteDateCodec`，新写入使用固定宽度亚秒时间，读取同时接受 V1.0.2 秒级字符串；history、capture event、summary 与 search candidate 排序改为直接比较规范化时间字符串，避免 SQLite `datetime()` 截断亚秒。ClipboardHistoryRepository、MigrationManager、AITokenUsageRepository 与 SQL builder 回归通过，既有 V1–V4 migration 未修改。
- SQLite current-mode/WAL 矩阵：默认 DELETE 模式通过写事务期间 committed-snapshot 搜索读取、竞争 writer 在 1,000 ms busy timeout 内失败、rollback、reopen 和关闭后无 WAL sidecar；合成 V1.0.2 build 4 数据集的 history、derived record、AI usage 与 V1–V4 幂等 migration 保持完整。隔离 WAL 实验的数据正确性和 busy 边界也通过，但关闭/reopen 后 `-wal` 与 `-shm` 仍存在，未通过本版本的 sidecar 生命周期门禁，因此 V1.0.3 保持 DELETE，不新增 checkpoint 或 migration。真实发布应用生成的 build 4 fixture 仍缺失，记录为 PD-006，Task 8.1 保持打开。
- V1.0.3 产品与文档收口：Info.plist 更新为 `1.0.3 (5)`，版本一致性脚本通过；中英文 README、CHANGELOG、V1.0.3 release notes、发布指南、人工 QA 模板、用户指南、隐私政策、架构与数据库文档同步最终实现和已知边界。实现未新增可见控件或字符串，既有 English、简体中文、繁体中文与 Accessibility 覆盖已在 Task 6.4 验证。PD-001 至 PD-007 均保留状态与影响，没有把签名、公证、真实旧库 fixture、人工 GUI smoke 或来源轮询限制虚假标记完成。
- 最终自动 QA：完整 macOS arm64 测试 368/368 通过、0 failure/0 skip；Debug build 通过；Xcode 文件引用 200 个、缺失 0 个；隐私日志扫描 125 个 Swift 文件、29 个 Logger 调用、直接 console 0；两项隐私用途说明和 `1.0.3 (5)` 版本一致性均通过；固定版本 OpenSpec strict validation 返回 change valid。Storage Reconciliation 独立审查另行执行 13 个定向测试并确认无阻断。真实旧库 fixture 仍为 PD-006，人工 GUI smoke 新增 PD-007，二者不包含在自动 QA 的完成声明中。
- 待决策事项：见 `docs/planning/PENDING_DECISIONS.md`。
