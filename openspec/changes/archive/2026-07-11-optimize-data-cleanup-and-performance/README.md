# optimize-data-cleanup-and-performance

Optimize long-running performance with startup cleanup, count and storage limits, database indexes, pagination, thumbnail cache improvements, polling frequency tuning, and memory checks.
