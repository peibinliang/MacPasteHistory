# bootstrap-macos-menubar-app

Build the macOS SwiftUI menu bar application foundation, app metadata, main/settings windows, Application Support storage, SQLite initialization, logging, and settings persistence.
