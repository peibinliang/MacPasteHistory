## 1. Settings UI

- [x] 1.1 Build the settings page layout.
  - 实现描述：设计设置窗口分区布局，包含基础设置、记录设置、清理设置和危险操作区域。
  - 前置条件：设置窗口入口已存在，基础 UI 组件风格已确定。
  - 验收条件：设置页可打开，控件布局清晰，窗口尺寸变化时内容不重叠。
- [x] 1.2 Add text recording switch.
  - 实现描述：添加文本记录开关，绑定到设置服务中的文本记录配置。
  - 前置条件：设置服务可读写布尔值，文本监听入口可查询该配置。
  - 验收条件：开关状态可保存；关闭后文本捕获流程读取到禁用状态。
- [x] 1.3 Add image recording switch.
  - 实现描述：添加图片记录开关，绑定到设置服务中的图片记录配置。
  - 前置条件：设置服务可读写布尔值，图片捕获入口可查询该配置。
  - 验收条件：开关状态可保存；关闭后图片捕获流程读取到禁用状态。
- [x] 1.4 Add startup launch switch.
  - 实现描述：提供开机启动开关，并调用 macOS 登录项相关 API 更新启动行为。
  - 前置条件：已确认目标 macOS 版本支持的登录项 API。
  - 验收条件：切换后设置状态持久化；系统登录项状态与 UI 尽量一致。
- [x] 1.5 Add Dock icon visibility switch.
  - 实现描述：提供 Dock 图标显示/隐藏配置，并通过应用 activation policy 调整显示行为。
  - 前置条件：已确认菜单栏工具默认 Dock 策略。
  - 验收条件：切换后 Dock 图标显示状态符合配置，重启后保持一致。

## 2. Retention And Limits

- [x] 2.1 Add history retention days setting.
  - 实现描述：添加历史保留天数输入控件，限制为合理正整数或预设选项。
  - 前置条件：设置服务支持数值配置和默认值。
  - 验收条件：保存后可读回；非法输入被阻止或恢复为有效值。
- [x] 2.2 Add text maximum record count setting.
  - 实现描述：添加文本最大保留数量设置，用于后续清理服务。
  - 前置条件：文本历史记录类型可区分，清理服务将读取该配置。
  - 验收条件：设置值持久化，并能被清理逻辑访问。
- [x] 2.3 Add image maximum record count setting.
  - 实现描述：添加图片最大保留数量设置，用于限制图片历史增长。
  - 前置条件：图片历史记录类型可区分，清理服务将读取该配置。
  - 验收条件：设置值持久化，并能被图片清理逻辑访问。
- [x] 2.4 Add single-image size limit setting.
  - 实现描述：添加单张图片最大大小设置，并提供 MB 等用户可理解单位。
  - 前置条件：图片捕获流程能检查图片大小。
  - 验收条件：设置值持久化；图片捕获时能读取并应用该限制。
- [x] 2.5 Add total storage cap setting.
  - 实现描述：添加总存储空间上限配置，用于图片文件和数据库清理策略。
  - 前置条件：存储目录大小可计算，清理服务将读取该配置。
  - 验收条件：设置值持久化，并能在清理服务中作为阈值使用。

## 3. Persistence And Application

- [x] 3.1 Persist all settings across restart.
  - 实现描述：统一保存设置项并在应用启动、窗口打开和服务运行时加载默认值。
  - 前置条件：所有设置控件和设置服务字段已定义。
  - 验收条件：修改任意设置后重启，UI 和运行行为都反映保存后的值。
- [x] 3.2 Apply text recording toggle to text capture.
  - 实现描述：在文本保存前检查文本记录开关，禁用时跳过读取后的持久化。
  - 前置条件：文本监听保存流程已存在，文本记录开关可读取。
  - 验收条件：关闭文本记录后复制文本不新增历史；开启后恢复正常。
- [x] 3.3 Apply image recording toggle to image capture.
  - 实现描述：在图片保存前检查图片记录开关，禁用时跳过图片文件和数据库写入。
  - 前置条件：图片捕获保存流程已存在，图片记录开关可读取。
  - 验收条件：关闭图片记录后复制图片不新增历史且不生成文件；开启后恢复正常。
- [x] 3.4 Apply limit settings to capture and cleanup services.
  - 实现描述：将保留天数、数量、图片大小和存储上限配置接入捕获与清理服务。
  - 前置条件：相关设置项已持久化，捕获/清理服务有统一配置读取入口。
  - 验收条件：修改限制后无需改代码即可影响后续捕获或清理行为。

## 4. Data Clearing

- [x] 4.1 Add a clear-all-data button.
  - 实现描述：在设置页危险操作区域添加清空全部数据按钮。
  - 前置条件：设置页布局已完成，数据清理服务有设计入口。
  - 验收条件：按钮可见且文案明确表示会删除本地历史数据。
- [x] 4.2 Confirm before destructive clearing.
  - 实现描述：点击清空前弹出确认对话框，要求用户明确确认后才执行。
  - 前置条件：清空按钮已存在，对话框组件或系统 alert 可用。
  - 验收条件：取消确认不会删除任何数据；确认后才进入清理流程。
- [x] 4.3 Clear database records and image files.
  - 实现描述：清空所有历史数据库记录，并删除 images/thumbnails 等本地图片文件。
  - 前置条件：repository 和图片存储服务支持批量删除。
  - 验收条件：清空后列表为空，数据库历史表无记录，图片文件目录无残留历史文件。
- [x] 4.4 Verify every setting persists and clear-all removes local data.
  - 实现描述：执行阶段 6 端到端验收，覆盖所有设置项、重启持久化和清空数据。
  - 前置条件：本阶段设置 UI、持久化、应用逻辑和清空任务已完成。
  - 验收条件：文档阶段 6 的验收标准全部通过。
