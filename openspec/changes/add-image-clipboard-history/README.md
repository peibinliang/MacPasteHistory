# add-image-clipboard-history

Implement image clipboard capture for PNG/TIFF/screenshots/browser images, local file storage, thumbnails, metadata, dedupe, restore to clipboard, deletion cleanup, and image recording settings.
